node_modules/
dist/
# 単体実行ファイル(PBI-0137。1 本 66MB。配布は Release の仕事)
adapters/official/*/bin/
coverage/
# broker(Rust)の build 成果物(PBI-0154。配布は Release の仕事。手元 cargo build の fallback 用)
broker/target/

.env*
*.log

.paa/
credentials.json

# a `paa` binary downloaded by the Quickstart into the repo root (never commit a 70MB binary)
/paa
/paa-darwin-*
/paa-linux-*
